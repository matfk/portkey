[server]
database = "/etc/portkey/portkey.db"
max_clock_skew = 60
max_ttl = 86400
cleanup_interval = 60
logs = "/var/log/portkey"
health_socket = "/var/run/portkey/health.sock"
nft_binary = "nft"

[logging]
level = "INFO"
